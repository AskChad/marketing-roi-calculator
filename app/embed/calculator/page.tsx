import EmbedCalculatorClient from '@/components/calculator/EmbedCalculatorClient'

export default function EmbedCalculatorPage() {
  // No authentication required for embed
  return <EmbedCalculatorClient />
}
